services:
  - type: web
    name: Telegram-Bot
    runtime: python
    buildCommand: apt-get update && apt-get install -y --no-install-recommends ffmpeg && rm -rf /var/lib/apt/lists/* && pip install -r requirements.txt
    startCommand: python -m bot.main
    healthCheckPath: /ready
    autoDeploy: true
